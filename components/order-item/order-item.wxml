<!-- 
  订单记录项目,包含不同的订单状态
 -->
<view class="order-item">
  <view class="top-bar">
    <image class="logo" src="{{order.logo}}"></image>
    <view class="content">
      <view class="line-one">
        <text class="name">{{order.name}}</text>
        <text class="check-status">{{order.checkStatus}}</text>
      </view>
      <view class="info">{{order.info}}</view>
    </view>
  </view>

  <view class="date-bar">
    <view class="date">{{order.checkInDate}} 入住</view>
    <view class="date">{{order.checkOutDate}} 退房</view>
  </view>

  <view class="bottom-bar">
    <view class="price">￥{{order.price}}</view>
    <view class="ctrl-bar">
      <text class="btn">评论</text>
      <text class="btn fill">已入住</text>
    </view>
  </view>
</view>