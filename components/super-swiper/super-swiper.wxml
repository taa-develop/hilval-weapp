<!-- 
  自定义轮播组件
 -->

<view
 class="super-swiper"
>
 <view class="container">
   <image
    wx:for="{{imgsStyled}}"
    wx:key="{{item.id}}"
    class="img {{item.typeClass}}"
    src="{{item.url}}"
   ></image>
 </view>
</view>
