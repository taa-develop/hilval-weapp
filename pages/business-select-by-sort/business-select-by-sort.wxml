<view class="sort">
  <view class="item {{item.value===props.select.sort.value?'active':''}}" wx:for="{{list}}" wx:key="{{index}}" data-obj="{{item}}"
    bind:tap="handleClick">{{item.name}}</view>
</view>