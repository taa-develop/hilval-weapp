<template name="history">
  <view class="c">
    <view class="name">{{name}}</view>
    <view class="l">
      <text class="t" wx:for="{{list}}" wx:key="{{index}}">{{item}}</text>
    </view>
  </view>
</template>